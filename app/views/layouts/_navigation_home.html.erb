<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">
    <img src="/assets/smyle-logo-horizontal.png" width="20%" height="20%" class="d-inline-block align-top" alt="">
  </a>
  
   <div id="navbarNavDropdown">
    <ul class="navbar-nav">

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Smyler Login
        </a>
        <div class="dropdown-menu bg-dark" aria-labelledby="navbarDropdownMenuLink">
          <%= form_for @smyler, url: '/sessions', html: {class: "form-inline"} do |f| %>
            <div class="input-group">
              <%= f.text_field :username %>
              <%= f.password_field :password %>
              <%= f.submit 'Login' %>
            </div>
          <% end %>
        </div>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Hearo Login
        </a>
        <div class="dropdown-menu bg-dark" aria-labelledby="navbarDropdownMenuLink">
          <%= form_for @hearo, url: '/sessions', html: {class: "form-inline"} do |f| %>
            <div class="input-group">
              <%= f.text_field :username %>
              <%= f.password_field :password %>
              <%= f.submit 'Login' %>
            </div>
          <% end %>
        </div>
      </li>

    </ul>
  </div>

</nav>
